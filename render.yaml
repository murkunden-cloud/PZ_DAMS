services:
  - type: web
    name: dams-pune-zone
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python init_database.py && gunicorn wsgi:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
